import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#2359a9;',
      light: '#009DE5',
      dark: '#002D59',
    },
    secondary: {
      main: '#2764be',
    },
  },
});

export default theme;